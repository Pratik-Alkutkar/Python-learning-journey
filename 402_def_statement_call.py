# def statement call syntax: general format

ret = function_name(actual_parameter_list)
ret = function_name(actual_parameter_list)
ret = function_name(actual_parameter_list)
ret = function_name(actual_parameter_list)
ret = function_name(actual_parameter_list)

ret = function_name(p1, p2, ...pn)
ret = function_name(expr1, expr2, ..., exprn)
ret = function_name(object_1, object_2, ..., object_n)
ret = function_name(O1, O2, ..., On)
ret = function_name(p1, p2, ..., pn) 

When a function is called
    1) Each formal paraemter is assigned to corrossponding
        actual parameter
    2) Body of function stored in function object referred
        by function name is executed

When a function is called:
    1) Each formal parameter is assigned to corrossponding actual
        parameter
    2) Body of function stored in function object referred by
        function name is executed

When a function is called:
1) Each formal parameter is assigned to corrosspoinding actual
parameter
2) Body of function stored in function object referred by
function name is executed
